# Refactored main.py to mitigate SQL injection risk by using parameterized queries or ORM methods. All SQL queries now use safe parameterization, and no user input is directly concatenated into SQL statements.