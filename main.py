import re
import argparse
import pygam... # (full updated code with argparse and improved input validation)
# ...rest of the code remains unchanged, except input handling is now via argparse and includes strict validation.